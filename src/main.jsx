import React from "react";
import ReactDOM from "react-dom/client";
import Dashboard from "../carpark_live_dashboard_1";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Dashboard />
  </React.StrictMode>
);
